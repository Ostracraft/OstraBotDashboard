{% extends '@layouts/base.html.twig' %}

{% block title %}Erreur {{ status_code }}: {{ status_text }}{% endblock title %}

{% block head %}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="/css/general.min.css">
{% endblock %}

{% block bodyClasses %}min-h-screen bg-gray-100 dark:bg-gray-700{% endblock %}
{% block body %}
    <div class="min-h-screen flex flex-wrap content-center justify-center">
        <div class="lg:max-w-xl bg-ostracraft rounded-3xl transform -rotate-6 py-2 contour-shadow dark:contour-dark-shadow">
            <div class="max-w-sm lg:max-w-xl bg-gray-200 dark:bg-gray-800 px-10 py-5 rounded-3xl grid md:grid-rows-loginModal grid-cols-1 grid-areas-loginModal text-black dark:text-white transform rotate-6 contour-shadow dark:contour-dark-shadow">
                <div class="grid-in-head">
                    <div class="bg-no-repeat h-12 w-48 md:h-20 md:w-72 bg-contain bg-ostrabot-banner-dark dark:bg-ostrabot-banner"></div>
                </div>
                <div class="grid-in-body md:mt-5 text-xs md:text-lg">
                    <p class="mb-3 ml-5 text-2xl"><span class="font-bold">Erreur {{ status_code }}:</span> {{ status_text }}</p>
                    <p class="mb-3 ml-5">
                        Il semblerait que vous ayez atteint une zone d'ombre !
                        Il serait plus judicieux de retourner <a href="{{ url("index:home", [], false) }}">en lieux sûr</a>.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <a class="toggle-theme" onclick="switchTheme()"><i class="fas fa-sun text-yellow-300 text-3xl"></i></a>
    <script>
        function switchTheme() {
            let html = document.querySelector('html');
            let button = document.querySelector('.toggle-theme');

            html.classList.toggle('dark');
            if (html.classList.contains('dark'))
                button.innerHTML = '<i class="fas fa-sun text-yellow-300 text-3xl"></i>';
            else
                button.innerHTML = '<i class="fas fa-moon text-gray-900 text-3xl"></i>';
        }
    </script>
{% endblock %}